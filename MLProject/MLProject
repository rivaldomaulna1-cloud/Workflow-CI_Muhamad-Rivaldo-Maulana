name: workflow ci

conda_env: null

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "dataset_preprocessing.csv"}
    command: "python modelling.py --data_path {data_path}"
